Address community directory feedback while preserving autonomous plugin creation on desktop and mobile.

- Remove script-resource creation from the chat's production React renderer with a versioned,
  documented dependency patch. Script preinitialization and rendering now fail explicitly.
- Compress the bundled WASM compiler and decompress it in memory using Web APIs. The package
  drops from about 21 MB to 7.4 MB and still compiles offline, including in WebKit.
- Add searchable settings definitions for Obsidian 1.13 while keeping the 1.12.4 settings fallback.
- Replace deprecated chat APIs, remove the CSS `!important`, and type the generated-function factory.
- Build, test and attest release assets in GitHub Actions before publishing them.

The manifest description was already corrected in 0.3.1.

### Remaining disclosures

The agent deliberately executes generated code and can enumerate, read and change vault files.
Streaming chat uses fetch with an Obsidian requestUrl fallback. These capabilities remain visible
to the reviewer; they have not been suppressed or claimed absent.

`main.js` still exceeds the 5 MB Obsidian Sync Standard limit. Install or update Superpowers on
each device with BRAT instead of relying on Sync to transfer the plugin file. Physical mobile
devices and live model inference have not been validated in these automated runs.

See `docs/community-submission.md` for review details and `docs/verification.md` for test coverage.
